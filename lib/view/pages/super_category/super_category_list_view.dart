import 'package:flutter/material.dart';

class SuperCategoryListView extends StatelessWidget {
  const SuperCategoryListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Super Cat');
  }
}
